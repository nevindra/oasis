[package]
name = "oasis-memory"
version = "0.1.0"
edition = "2021"

[dependencies]
oasis-core = { path = "../oasis-core" }
libsql = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
