// Package processor provides helpers for composing core.PreProcessor,
// core.PostProcessor, and core.PostToolProcessor implementations into
// pipelines that the agent loop can invoke at each hook point.
package processor
